# ポケモンクイズアプリ 仕様書

## 1. 概要

ポケモン名の一部を伏せた穴あき問題から、該当するポケモンを答えるクイズアプリ。スマホでのプレイを想定。

## 2. 出題仕様

### 2.1 出題形式

- ポケモン名の一部の文字を「◯」で伏せた穴あき形式
- 例: 「◯◯チュ◯」（ピカチュウ、ライチュウなどが該当）
- 出題内容はランダムに選択する

### 2.2 穴あけのルール

- 表示する文字数は 1 文字以上、全体文字数の半数（切り捨て）以下からランダムに決定
  - 例: 2 文字なら 1 文字、3 文字なら 1 文字、4 文字なら 1〜2 文字、5 文字なら 1〜2 文字、6 文字なら 1〜3 文字
- 残りの文字を穴とする
- 穴の位置はランダム
- 該当ポケモンが一意に定まる必要はない（複数該当で OK）
- ポケモン名が 2 文字未満の場合は出題対象外とする（実データ上は該当なしの想定）

### 2.3 文字数のカウントルール

- `Intl.Segmenter` による grapheme 単位での文字数カウントとする
- 拗音・濁点・半濁点・長音・特殊文字（数字、記号、英字など）も grapheme 単位で 1 文字として扱う
- これらはすべて穴 1 つで表現する

### 2.4 出題対象ポケモン

- 現在判明している全ポケモン
- 通常フォルムのみ（リージョンフォーム、フォルムチェンジは対象外）
- 特殊文字を含むポケモンも対象に含む（ポリゴン２、タイプ：ヌル、ニドラン♀♂ など）

### 2.5 出題ロジック

- 出題のたびに全ポケモンからランダムに 1 体を選び、その名前から穴パターンを生成して出題する
- 出題対象ポケモンの選出に偏りなどは考慮しない
- 同じポケモンが繰り返し出題されることを制限する仕組みは設けない

### 2.6 出題情報の URL 保存

- 出題されたポケモンと穴パターン（最初にランダム表示された文字）を URL の Query Parameter に保存する
- ヒントで開示した文字は URL に含めない
- パラメータ名は `q`、値は自作 64 進数でエンコードした 3 文字の文字列（例: `?q=A3m`）
- ページ読み込み時に `?q=` パラメータが存在すれば、そのパラメータから問題を復元する
  - 復元できない（不正な値など）場合はランダムに問題を生成する
- 次の問題への遷移時に URL を新しい問題のパラメータで上書きする（`history.replaceState`）

#### エンコード形式

出題情報を 17 bit の整数値にパックし、自作 64 進数で 3 文字にエンコードする。

```
value = (revealed_mask << 11) | (pokedex_number - 1)
```

| ビット | 内容                                                         |
| ------ | ------------------------------------------------------------ |
| 0–10   | 図鑑番号 − 1（0–1024）                                       |
| 11–16  | revealed ビットマスク（bit i = 1 のとき i 番目の文字が表示） |

- 自作 64 進数の文字セットは URL-safe な 64 文字（`0–9`, `a–z`, `A–Z`, `-`, `_` を独自順序で配置）
- 64³ = 262,144 > 最大値 130,048 のため、常に 3 文字で表現できる

## 3. 回答仕様

### 3.1 入力方法

- タイピング入力
- 入力欄にはユーザーが入力したそのままの文字を表示
- 送信時に以下の変換を行ってから判定
  - ひらがな → カタカナ（文字コードのシフトによる変換）
  - 半角 ASCII 印字可能文字（`!-~`）→ 全角大文字（文字コードのシフトによる変換）
- 変換後の入力に応じて以下のエラーを表示
  - いずれのポケモン名にも該当しない場合: 「未知のポケモンです」
  - ポケモン名には該当するが穴パターンと一致しない場合: 「パターンにマッチしません」
- 解答の送信は Enter キーまたは送信ボタンで行う
- 入力欄のフォーカスはユーザーが行う

### 3.2 正解条件

- 出題された穴パターンに該当するポケモンのうち、いずれか 1 体の公式名を入力すれば正解
  - 「穴パターンに該当する」とは、元の表示文字（最初にランダム表示された文字）と一致することを指す（ヒントで開示した文字は判定に含めない）
- 公式表記での完全一致のみ正解とする（送信時のひらがな→カタカナ変換後に判定）
- 正解判定の対象は全ポケモンの一覧

### 3.3 補助機能

#### 3.3.1 ヒント機能

- 「◯」をタップ（クリック）すると、その位置の文字が開示される
- ヒントの開示元ポケモンは出題対象ポケモンと同一（出題時に決定される）
- ユーザーがタップした穴の位置の文字を、開示元ポケモンから取得して表示する
- 一度開いたヒントは戻せない
- ヒントの使用回数に制限はない
- タップの判定は `click` イベントを使用する

#### 3.3.2 パス機能

- 正解を表示する
- パス時には特別な状態の記録は行わない

### 3.4 答え合わせ

- 正解、もしくはパス時に該当ポケモン全員の情報を表示する
  - 「該当ポケモン」はヒントで開示した文字を考慮せず、元の穴パターン（最初にランダム表示された文字）のみに基づく
- 該当ポケモンの名前を一覧形式で表示し、画像は表示しない
- 名前にはポケモン公式の全国図鑑へのアンカーリンクを付与する
  - URL 形式: `https://zukan.pokemon.co.jp/detail/{図鑑番号を 4 桁ゼロ埋め}`
- 表示順は図鑑番号順とする

### 3.5 画面遷移

- 答え合わせから次の問題への遷移はボタン操作で行う（自動遷移しない）
- 答え合わせ画面の表示は画面の切り替えで行い、ページ遷移（リロード）はしない

## 4. ゲーム進行

- エンドレス形式（制限時間なし、ラウンド制なし）
- スコアや連続正解数の記録はなし
- 難易度設定なし

## 5. データソース

- PokéAPI（公式 GraphQL エンドポイント）から事前にデータフェッチ
- フェッチ結果を JSON として保持し、アプリに同梱
- データ更新は手動実行
- 取得が必要な情報:
  - ポケモン名（日本語/カタカナ、`ja-Hrkt`）
- 取得対象は `is_default = true` の pokemon-species に限定する
- JSON にはポケモン名のみを図鑑番号順の配列として格納する
  - 図鑑番号はインデックス + 1 として算出する（連番性はフェッチ時に検証）

## 6. プラットフォーム

- スマホ（モバイルブラウザ想定）
- ブラウザサポートは Baseline Widely Available を基準とする
- 表示言語は日本語固定
