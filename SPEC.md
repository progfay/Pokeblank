# ポケモンクイズアプリ 仕様書

## 1. 概要

ポケモン名の一部を伏せた穴あき問題から、該当するポケモンを答えるクイズアプリ。スマホでのプレイを想定。

## 2. 画面構成

### 2.1 トップページ（`/`）

- 「Pokéblank」ロゴ（テキスト）を中央に表示
- モード選択を並べて表示する
  - 「Endless Mode」へのリンク（`/endless/`）
  - 「Time Attack Mode」へのリンク（`/timeattack/`）
- クレジット（著作権表示・免責）を画面下部に表示

### 2.2 Endless Mode（`/endless/`）

エンドレス形式のクイズ。詳細は「3. 出題仕様」以降を参照。

### 2.3 Time Attack Mode（`/timeattack/`）

5 問 1 セットのタイムアタック形式のクイズ。5 問を解ききるまでのタイムを計測する。詳細は「8. Time Attack 仕様」を参照。

## 3. 出題仕様

### 3.1 出題形式

- ポケモン名の一部の文字を「◯」で伏せた穴あき形式
- 例: 「◯◯チュ◯」（ピカチュウ、ライチュウなどが該当）
- 出題内容はランダムに選択する

### 3.2 穴あけのルール

- 表示する文字数は 1 文字以上、全体文字数の半数（切り捨て）以下からランダムに決定
  - 例: 2 文字なら 1 文字、3 文字なら 1 文字、4 文字なら 1〜2 文字、5 文字なら 1〜2 文字、6 文字なら 1〜3 文字
- 残りの文字を穴とする
- 穴の位置はランダム
- 該当ポケモンが一意に定まる必要はない（複数該当で OK）
- ポケモン名が 2 文字未満の場合は出題対象外とする（実データ上は該当なしの想定）

### 3.3 文字数のカウントルール

- `Intl.Segmenter` による grapheme 単位での文字数カウントとする
- 拗音・濁点・半濁点・長音・特殊文字（数字、記号、英字など）も grapheme 単位で 1 文字として扱う
- これらはすべて穴 1 つで表現する

### 3.4 出題対象ポケモン

- 現在判明している全ポケモン
- 通常フォルムのみ（リージョンフォーム、フォルムチェンジは対象外）
- 特殊文字を含むポケモンも対象に含む（ポリゴン２、タイプ：ヌル、ニドラン♀♂ など）

### 3.5 出題ロジック

- 出題のたびに全ポケモンからランダムに 1 体を選び、その名前から穴パターンを生成して出題する
- 出題対象ポケモンの選出に偏りなどは考慮しない
- 同じポケモンが繰り返し出題されることを制限する仕組みは設けない

### 3.6 出題情報の URL 保存

- 出題されたポケモンと穴パターン（最初にランダム表示された文字）を URL の Query Parameter に保存する
- ヒントで開示した文字は URL に含めない
- パラメータ名は `q`、値は自作 64 進数でエンコードした 3 文字の文字列（例: `?q=A3m`）
- ページ読み込み時に `?q=` パラメータが存在すれば、そのパラメータから問題を復元する
  - 復元できない（不正な値など）場合はランダムに問題を生成する
- 次の問題への遷移時に URL を新しい問題のパラメータで上書きする（`history.replaceState`）

#### エンコード形式

出題情報を 17 bit の整数値にパックし、自作 64 進数で 3 文字にエンコードする。

```
value = (revealed_mask << 11) | (pokedex_number - 1)
```

| ビット | 内容                                                         |
| ------ | ------------------------------------------------------------ |
| 0–10   | 図鑑番号 − 1（0–1024）                                       |
| 11–16  | revealed ビットマスク（bit i = 1 のとき i 番目の文字が表示） |

- 自作 64 進数の文字セットは URL-safe な 64 文字（`0–9`, `a–z`, `A–Z`, `-`, `_` を独自順序で配置）
- 64³ = 262,144 > 最大値 130,048 のため、常に 3 文字で表現できる

## 4. 回答仕様

### 4.1 入力方法

- タイピング入力
- 入力欄にはユーザーが入力したそのままの文字を表示
- 送信時に以下の変換を行ってから判定
  - ひらがな → カタカナ（文字コードのシフトによる変換）
  - 半角 ASCII 印字可能文字（`!-~`）→ 全角大文字（文字コードのシフトによる変換）
- 変換後の入力に応じて以下のエラーを表示
  - いずれのポケモン名にも該当しない場合: 「未知のポケモンです」
  - ポケモン名には該当するが穴パターンと一致しない場合: 「パターンにマッチしません」
- 解答の送信は Enter キーまたは送信ボタンで行う
- 入力欄のフォーカスはユーザーが行う
  - Tab キーによるフォーカス移動では、入力欄が最初のフォーカス対象となる

### 4.2 正解条件

- 出題された穴パターンに該当するポケモンのうち、いずれか 1 体の公式名を入力すれば正解
  - 「穴パターンに該当する」とは、元の表示文字（最初にランダム表示された文字）と一致することを指す（ヒントで開示した文字は判定に含めない）
- 公式表記での完全一致のみ正解とする（送信時のひらがな→カタカナ変換後に判定）
- 正解判定の対象は全ポケモンの一覧

### 4.3 補助機能

#### 4.3.1 ヒント機能

- 「◯」をタップ（クリック）すると、その位置の文字が開示される
- ヒントの開示元ポケモンは出題対象ポケモンと同一（出題時に決定される）
- ユーザーがタップした穴の位置の文字を、開示元ポケモンから取得して表示する
- 一度開いたヒントは戻せない
- ヒントの使用回数に制限はない
- タップの判定は `click` イベントを使用する

#### 4.3.2 パス機能

- 正解を表示する
- パス時には特別な状態の記録は行わない

### 4.4 答え合わせ

- 正解、もしくはパス時に該当ポケモン全員の情報を表示する
  - 「該当ポケモン」はヒントで開示した文字を考慮せず、元の穴パターン（最初にランダム表示された文字）のみに基づく
- 該当ポケモンの名前を一覧形式で表示し、画像は表示しない
- 名前にはポケモン公式の全国図鑑へのアンカーリンクを付与する
  - URL 形式: `https://zukan.pokemon.co.jp/detail/{図鑑番号を 4 桁ゼロ埋め}`
- 表示順は図鑑番号順とする

### 4.5 画面遷移

- 答え合わせから次の問題への遷移はボタン操作で行う（自動遷移しない）
- 答え合わせ画面の表示は画面の切り替えで行い、ページ遷移（リロード）はしない
- 答え合わせ画面では、Tab キーによるフォーカス移動で Next ボタンが最初のフォーカス対象となる

## 5. ゲーム進行（Endless Mode）

- エンドレス形式（制限時間なし、ラウンド制なし）
- スコアや連続正解数の記録はなし
- 難易度設定なし

Time Attack Mode のゲーム進行については「8. Time Attack 仕様」を参照。

## 6. データソース

- PokéAPI（公式 GraphQL エンドポイント）から事前にデータフェッチ
- フェッチ結果を JSON として保持し、アプリに同梱
- データ更新は手動実行
- 取得が必要な情報:
  - ポケモン名（日本語/カタカナ、`ja-Hrkt`）
- 取得対象は `is_default = true` の pokemon-species に限定する
- JSON にはポケモン名のみを図鑑番号順の配列として格納する
  - 図鑑番号はインデックス + 1 として算出する（連番性はフェッチ時に検証）

## 7. プラットフォーム

- スマホ（モバイルブラウザ想定）
- ブラウザサポートは Baseline Widely Available を基準とする
- 表示言語は日本語固定

## 8. Time Attack 仕様

Endless Mode と共通の仕様（出題形式・穴あけルール・文字数カウント・出題対象ポケモン・回答変換ルール・該当ポケモン判定・公式図鑑リンクなど）は「3. 出題仕様」「4. 回答仕様」をそのまま踏襲する。本章では Time Attack 固有の差分のみを定義する。

### 8.1 ゲーム進行

- 1 セットは 5 問固定
- スキップした問もセットの 5 問に含める（スキップ込みで 5 問進めば終了）
- 一度進んだ問題には戻れない
- 各問は「正解」または「スキップ」で即座に次の問題へ遷移する
  - Endless Mode のような答え合わせ画面は各問では表示しない
- 5 問終了後に結果画面を表示する

### 8.2 計測

- タイマーの計測開始は Start ボタンを押した瞬間とする
  - `/timeattack/` に遷移した時点では計測を開始せず、Start ボタンの押下を待つ
- タイマーの計測終了は 5 問目を正解した瞬間とする
- プレイ中のタイマー表示は「ペナルティ込みの合計タイム」のみを表示する
- 内部的には「実測タイム（ストップウォッチ）」と「合計タイム（実測 + ペナルティ）」を別管理とする
  - 実測タイム: Start から 5 問目正解までの経過時間
  - 合計タイム: 実測タイム + ペナルティ合計

### 8.3 ペナルティ

- ヒントを 1 回使用するごとに +10 秒
- スキップを 1 回使用するごとに +30 秒
- ペナルティは合計タイムに加算する（実測タイムには加算しない）
- ペナルティ加算時の挙動
  - タイマー表示（合計タイム）はその場で跳ねる（加算後の値に即時更新）
  - 加算演出として、タイマー付近に「+10s」「+30s」のラベルがポップアップして消える

### 8.4 各問の出題・回答

- 出題形式・穴あけルール・文字数カウント・出題対象ポケモン・穴パターン生成は「3. 出題仕様」と同一
- 回答の入力方法・送信時の変換・エラー表示・正解条件は「4. 回答仕様」と同一
- ヒント機能は「4.3.1 ヒント機能」と同一（タップした穴の位置の文字を開示元ポケモンから開示）
  - ただし Time Attack ではヒント 1 回につき +10 秒のペナルティが発生する
  - ヒントで開示した文字は正解判定・URL 保存・該当ポケモン判定のいずれにも含めない（Endless と同じ方針）

### 8.5 スキップ機能

- Time Attack のスキップは「4.3.2 パス機能」を置き換えるものとする
- スキップした問題は不正解確定とし、答え合わせを表示せず即座に次の問題へ遷移する
- スキップ 1 回につき +30 秒のペナルティが発生する
- スキップした問はもう戻せない

### 8.6 出題情報の URL 保存

- 5 問分の出題情報を URL の Query Parameter に保存する
- 各問のエンコードは「3.6 出題情報の URL 保存 / エンコード形式」と同一の 17 bit 形式（自作 64 進数 3 文字）を流用する
  - `value = (revealed_mask << 11) | (pokedex_number - 1)`
  - revealed ビットマスクは「最初にランダム表示された文字」のみを表す。ヒントで開示した文字は含めない
- 5 問分（各 3 文字）を連結した 15 文字の文字列を 1 つの値として保存する
- Endless Mode（`/endless/?q=`）と Time Attack Mode は新パス `/timeattack/` で区別する
  - パラメータ名は `q`（Endless と共通。値の長さで形式が異なる）
- ページ読み込み時にパラメータが存在すれば、その 5 問を復元する
  - 復元できない（文字数不一致・不正な値など）場合はランダムに 5 問を生成する
- セット内の問題は固定であり、進行に応じた URL の上書きは行わない（共有された URL を開けば常に同じ 5 問になる）

### 8.7 共有 URL からのプレイ（再挑戦）

- 共有された URL を開いた場合も、同じ 5 問で改めてタイムを競う（計測あり）
- 共有 URL を開いた場合も通常プレイと同様に Start ボタンを押させ、タイマーは 0 から計測を開始する

### 8.8 結果画面

5 問終了後に表示する。ページ遷移（リロード）はせず画面の切り替えで表示する。

- ペナルティ込みの合計タイムを表示する
- 各問の結果一覧を表示する
  - 各問について、その穴パターンに該当する全ポケモン（複数いれば全員）を表示する
    - 「該当ポケモン」はヒントで開示した文字を考慮せず、元の穴パターン（最初にランダム表示された文字）のみに基づく（「4.4 答え合わせ」と同一）
  - スキップした問についても該当ポケモン（正解）を表示する
  - 各問について「スキップした問」「ヒントを使用した問」であることを区別して表示する
  - 該当ポケモンの名前は一覧形式で表示し、画像は表示しない
  - 名前にはポケモン公式の全国図鑑へのアンカーリンクを付与する
    - URL 形式: `https://zukan.pokemon.co.jp/detail/{図鑑番号を 4 桁ゼロ埋め}`
  - 各問内の該当ポケモンの表示順は図鑑番号順とする
- 導線ボタン
  - 「共有」ボタン: 現在の 5 問の URL を共有する
    - Web Share API を使用し、未対応の環境ではクリップボードコピーにフォールバックする
  - 「新規」ボタン: 新しい 5 問を生成して新しいセットを開始する
  - リトライ（同じ問題への再挑戦）ボタンは設けない

### 8.9 画面遷移・フォーカス

- 結果画面から次のセットへの遷移はボタン操作で行う（自動遷移しない）
- 結果画面の表示は画面の切り替えで行い、ページ遷移（リロード）はしない
- 各問のプレイ画面では、Tab キーによるフォーカス移動で入力欄が最初のフォーカス対象となる（「4.1」と同一）
